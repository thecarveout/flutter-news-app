# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - main # Ensure this matches your main branch name (e.g., 'main' or 'master')

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # --- ADD THIS SECTION HERE ---
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable' # Use 'stable' unless you specifically develop on 'beta' or 'dev'
      # ---------------------------

      - name: Get Flutter dependencies # Good practice to ensure all packages are available
        run: flutter pub get

      - name: Build Flutter web app
        run: flutter build web --release

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          # Make sure this secret name exactly matches what you set in GitHub Secrets
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_THECARVEOUT_CE0F3 }}
          channelId: live
          projectId: thecarveout-ce0f3
